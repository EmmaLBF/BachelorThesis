Benchmarks (not the ones with infinite lists)
Sorting Algorithms (merge sort)
- Measure time in AbsLang eval, CLang eval and compiled C

Fix current edge cases

See current optimization opportunities

General functional programming optimisations

ADTs

What information is lost when compiling to C? How would this generate faster code? 
- Annotations in LLVM which we know are true 

- Try to get smaller programs to LLVM

Runtime: Garbage collector, "Machine" Spineless tagless G machine, stack lay out etc


Language stuff (does not theoretically increase power)
 parsing (parser combinators), patterns matching, types, type classes